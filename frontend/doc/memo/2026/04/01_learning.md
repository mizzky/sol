# 学習記録 2026-04-01

## セッション 1 (05:12)

### 取り組んだタスク
- フロントエンド：共通UIコンポーネントの導入（Tailwindベース）、多ページのUI統一。
- テスト対応：Jestで失敗していたテスト（orders heading、product placeholder）を修正して全テストを通した。
- バックエンド接続調査：ブラウザでのCORS/オリジン差異（localhost vs 127.0.0.1）を検証。Next.jsのrewritesで同一オリジンプロキシを導入して `/api/*` をバックエンドへルーティングする案を検証・実装。
- デバッグ：バックエンドでの一時的なCORS追加（127.0.0.1）を検証（ユーザーが保持を拒否）。最終的にユーザーがブラウザのキャッシュ削除で接続問題が解決したことを確認。

### ユーザーが質問した内容
- バックエンドとの接続が上手くいっていません。原因を調査して修正してください
- VS Codeのポート自動転送が多数表示されるが、これは何か？

### 躓いたポイントと解決策
- 問題: ブラウザのオリジン差（`localhost` と `127.0.0.1`）が原因でCORSプリフライトが失敗していた。
  - 対処: 開発用としてNext.jsのproxy（rewrites）で同一オリジン化し、ブラウザからは `http://localhost:3000` のみを使う方式を検証。バックエンド側の一時的な127.0.0.1追加は検証のみで保持しない方針。
- 最終的な即時解決: ユーザーがブラウザのキャッシュを削除したら問題が解消。
  - 注意: 開発者向けに「localhost:3000 を使用」「必要ならキャッシュをクリア」の手順をドキュメント化すること。

### 次回課題
- リポジトリに残っている一時的なCORSの変更があればリバートする（要確認）。
- `next.config.ts` のプロキシ設定をコミットして開発手順（README/開発メモ）に追記する。
- 本番/ステージングへのCORSポリシーを確認し、起動時のオリジン条件を明文化する。
- 必要なら、キャッシュ関連のトラブルシューティング手順を短くまとめる。

---

Generated from session notes and interactive debugging (journal file could not be read for full raw checkpoints; script run succeeded and printed journal path).
ログ作成者: システム（LLM）
作成日時: 2026-04-01 05:12

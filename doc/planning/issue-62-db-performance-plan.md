# Issue #62 大量データ下のEC主要クエリ最適化・実行計画検証

## 1. 目的

性能検証専用DB `coffeesys_perf` に再現可能な大量データを投入し、ECの主要クエリを `EXPLAIN ANALYZE` とアプリケーション側の計測結果に基づいて評価する。

対象は商品一覧、ユーザー別注文一覧、注文明細、カート取得、N+1比較、DB接続プールである。推測で最適化せず、before/afterの実行計画と計測値を根拠に変更の採否を判断する。

## 2. スコープと固定方針

- PostgreSQL 17、Gin、`database/sql`、sqlcの現行構成を維持する。
- 性能検証には開発DBと分離した `coffeesys_perf` を使用する。
- 性能DB内の業務データは、DB名を検証した後で全削除可能とする。
- OFFSET/keyset paginationの比較は検証専用とし、本番APIやフロントエンドには導入しない。
- N+1比較コードは本番処理と分離する。
- GitHub Pages向け成果物は `doc/` 配下のMarkdownまでとし、公開設定は対象外とする。
- 1ミニタスクを30〜60分、原則1コミットで進める。
- small、medium、largeの順に検証し、前段階が完了するまで次の規模へ進まない。
- 最初に着手するタスクは **0-1 性能DBの接続確認** とする。

## 3. データプロファイル

| Profile | users | products | orders | order_items | 用途 |
|---|---:|---:|---:|---:|---|
| small | 1,000 | 10,000 | 10,000 | 30,000 | 安全性・整合性・再実行性の確認 |
| medium | 10,000 | 100,000 | 100,000 | 計測手順と劣化傾向の確認 |
| large | 100,000 | 1,000,000 | 1,000,000 | Issue #62の本計測 |

追加のデータ分布は次のとおりとする。

- 1注文につき注文明細3件を基本とする。
- 特定の検証用ユーザーへ十分な注文を集中させ、深いOFFSETを再現する。
- `created_at`が同じ注文を意図的に含め、複合ソートとkeysetの境界を検証する。
- status、カテゴリ、在庫、商品利用可否に偏りを持たせる。
- cartsはユーザーごとに最大1件、cart_itemsは1カート3件を基本とする。
- 乱数ではなく連番と剰余を用い、同じprofileから同じ分布を再生成する。

## 4. ミニタスク

### Phase 0: 作業準備

#### 0-1. 性能DBの接続確認（30分）

- **依存:** なし
- **作業:** `PERF_DATABASE_URL`の存在を値を表示せず確認し、`current_database()`、migration version、主要テーブルの現在件数を取得する。
- **完了条件:** 接続先が `coffeesys_perf` であり、開発DBへ接続していないと確認できる。
- **成果物:** 引き継ぎジャーナルのセッション更新。

#### 0-2. 作業ブランチと配置方針（30分）

- **依存:** 0-1
- **作業:** ブランチ `feat/issue-62-db-performance` を用意し、検証SQL、性能テスト、計測結果の配置を確定する。
- **完了条件:** `.env`や接続情報がGit管理対象外で、追加物の責務が明確になっている。
- **成果物:** ディレクトリ構成と作業規約。

### Phase 1: smallデータ投入

#### 1-1. データ件数と分布の仕様化（45分）

- **依存:** 0-2
- **作業:** 各profileの件数、集中ユーザー、日時重複、status比率、整合性条件を定義する。
- **完了条件:** seed SQLの入力値と期待値が確定している。

#### 1-2. DB名安全ガード作成（45分）

- **依存:** 1-1
- **作業:** `coffeesys_perf`以外では失敗する確認処理と、業務テーブルのみを初期化する処理を作る。
- **完了条件:** 開発DBへの誤投入・誤削除を防止できる。

#### 1-3. users・categories投入（45分）

- **依存:** 1-2
- **作業:** deterministicなusersとcategoriesを生成し、件数と一意性を検証する。
- **完了条件:** 再投入時にも同じデータ分布になる。

#### 1-4. products投入（60分）

- **依存:** 1-3
- **作業:** smallの10,000商品を生成し、SKU、価格、カテゴリ、在庫に分布を持たせる。
- **完了条件:** SKU重複と外部キー孤児がなく、商品一覧計測が可能である。

#### 1-5. carts・cart_items投入（45分）

- **依存:** 1-4
- **作業:** カートと明細を生成し、UNIQUE制約と外部キーを検証する。
- **完了条件:** 既存カートJOINを計測できる。

#### 1-6. orders投入（60分）

- **依存:** 1-4
- **作業:** smallの10,000注文を生成し、集中ユーザー、日時重複、statusの偏りを作る。
- **完了条件:** OFFSETと複合インデックスを検証できる分布になっている。

#### 1-7. order_items・注文合計投入（60分）

- **依存:** 1-6
- **作業:** 1注文3明細、合計30,000件を生成し、注文合計と明細合計を一致させる。
- **完了条件:** 数量、価格、外部キー、注文合計の検証が通る。

#### 1-8. 再実行性とANALYZE確認（45分）

- **依存:** 1-5、1-7
- **作業:** seedを2回実行し、件数が増殖しないことを確認してから `ANALYZE` を実行する。
- **完了条件:** smallを安全に再構築でき、投入時間とDBサイズが記録されている。

### Phase 2: medium・large展開

#### 2-1. profile切り替え対応（60分）

- **依存:** 1-8
- **作業:** `small / medium / large`をpsql変数で選択可能にし、不正なprofileをエラーにする。
- **完了条件:** smallの結果を変えずに件数だけを切り替えられる。

#### 2-2. medium投入検証（60分）

- **依存:** 2-1
- **作業:** mediumを投入し、件数、整合性、投入時間、DBサイズを記録する。
- **完了条件:** largeへ進める安全性と処理時間の見通しが得られている。

#### 2-3. large商品データ投入（60分目安）

- **依存:** 2-2
- **作業:** users 100,000件、products 1,000,000件を投入する。
- **完了条件:** SKU重複がなく、カテゴリ分布と件数が期待どおりである。

#### 2-4. large注文データ投入（60分目安）

- **依存:** 2-3
- **作業:** orders 1,000,000件を投入する。
- **完了条件:** 集中ユーザーの注文数と日時分布が期待どおりである。

#### 2-5. large注文明細投入（60分目安）

- **依存:** 2-4
- **作業:** order_items 3,000,000件を投入し、整合性検査と `ANALYZE` を実行する。
- **完了条件:** 注文合計、孤児レコード、投入時間、DBサイズを確認できる。

large投入が60分を超えた場合は処理を途中で壊さず、実所要時間そのものを計測結果として残す。

### Phase 3: before計測

#### 3-1. 計測条件テンプレート作成（45分）

- **依存:** 2-5
- **作業:** SQL、パラメータ、件数、インデックス、PostgreSQLバージョン、実行回数の記録形式を作る。
- **完了条件:** `EXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)`の保存形式が統一されている。

#### 3-2. 商品一覧before計測（45分）

- **依存:** 3-1
- **作業:** 既存全件取得をsmall、medium、largeで計測する。
- **完了条件:** scan type、actual time、rows、buffersが保存されている。

#### 3-3. 注文一覧before計測（60分）

- **依存:** 3-1
- **作業:** 集中ユーザーに対しOFFSET `0 / 1,000 / 10,000 / 100,000`を計測する。
- **完了条件:** 現在の複合インデックス利用状況とOFFSET増加による変化が保存されている。

#### 3-4. 注文明細before計測（45分）

- **依存:** 3-1
- **作業:** 明細数の異なる注文を選び、`order_id`検索を計測する。
- **完了条件:** `idx_order_items_order_id`の利用状況が保存されている。

#### 3-5. カートbefore計測（45分）

- **依存:** 3-1
- **作業:** 既存JOINクエリを計測する。
- **完了条件:** carts、cart_items、productsのアクセス方法が保存されている。

### Phase 4: クエリ・インデックス改善

#### 4-1. 商品keyset比較（45分）

- **依存:** 3-2
- **作業:** `WHERE id > cursor ORDER BY id LIMIT 50`を検証し、全件取得と比較する。
- **完了条件:** 読み取り行数と時間差が記録され、本番APIを変更していない。

#### 4-2. 注文keyset比較（60分）

- **依存:** 3-3
- **作業:** `(created_at, id)`をカーソルにし、OFFSETと同じ開始位置・件数で比較する。
- **完了条件:** 結果の重複・欠落がなく、実行計画の差を説明できる。

#### 4-3. 注文順序のテスト作成（45分）

- **依存:** 4-2
- **作業:** `created_at`が同じ注文は`id DESC`になる期待値を作り、既存SQLで失敗を確認する。
- **完了条件:** 決定的ソートを要求するRedテストがある。

#### 4-4. 注文クエリと複合インデックス変更（60分）

- **依存:** 4-3
- **作業:** `ORDER BY created_at DESC, id DESC`へ変更し、インデックスを `(user_id, created_at DESC, id DESC)`へ置換してsqlcを再生成する。
- **完了条件:** テストがGreenになり、migrationのup/downが通る。

#### 4-5. 重複インデックスの確認（45分）

- **依存:** 3-2、3-5
- **作業:** UNIQUE制約由来のインデックスと既存インデックスについて、サイズ、カラム順、利用クエリを比較する。
- **完了条件:** 削除候補と維持対象が実測根拠付きで確定している。

#### 4-6. 重複インデックス整理（45分）

- **依存:** 4-5
- **作業:** 根拠が確認できた重複インデックスだけをmigrationで削除する。
- **完了条件:** up/down後のスキーマ、容量差、書き込みへの副作用が記録されている。

#### 4-7. after実行計画取得（60分）

- **依存:** 4-1、4-4、4-6
- **作業:** 商品、注文、注文明細のafterを取得する。
- **完了条件:** before/afterのscan type、rows、actual time、buffersを比較できる。

### Phase 5: N+1比較

#### 5-1. N+1性能テスト設計（45分）

- **依存:** 2-5
- **作業:** N=`10 / 100 / 500`、N+1版の期待クエリ数`1 + N`、batch版の期待クエリ数2を定義する。
- **完了条件:** 結果同値性とクエリ回数のテスト条件が決まっている。

#### 5-2. 意図的なN+1版作成（60分）

- **依存:** 5-1
- **作業:** 注文一覧取得後、ループ内で明細を取得する検証専用コードを作る。
- **完了条件:** クエリ回数と取得結果を記録できる。

#### 5-3. batch版作成（60分）

- **依存:** 5-2
- **作業:** order_id配列で明細を一括取得する検証専用コードを作る。
- **完了条件:** N+1版と結果が一致し、クエリ回数が2になる。

#### 5-4. N+1実測（45分）

- **依存:** 5-3
- **作業:** 各Nをウォームアップ後5回実行し、中央値、クエリ回数、取得行数を記録する。
- **完了条件:** 件数増加に伴う差を説明できる。実行時間の大小は自動テストの合否条件にしない。

### Phase 6: DB接続プール

#### 6-1. 接続プール設定テスト（60分）

- **依存:** 0-2
- **作業:** 既定値、環境変数上書き、不正値、負数、idle > openのテストを作る。
- **完了条件:** 設定実装前のRedを確認できる。

#### 6-2. 接続プール設定実装（60分）

- **依存:** 6-1
- **作業:** MaxOpen=`10`、MaxIdle=`5`、MaxLifetime=`30m`を `sql.Open`後、`PingContext`前に適用する。
- **完了条件:** テストがGreenになり、設定値の根拠が記録されている。

#### 6-3. 簡易負荷テスト作成（60分）

- **依存:** 6-2、2-5
- **作業:** 並行数`1 / 5 / 10 / 20`でDBクエリを実行し、`DBStats`を取得する。
- **完了条件:** 処理時間、`WaitCount`、`WaitDuration`、使用中接続数を出力できる。

#### 6-4. 接続数比較（45分）

- **依存:** 6-3
- **作業:** MaxOpenを`2 / 5 / 10 / 20`で比較する。
- **完了条件:** 既定値10の採用理由と、接続数を増やしすぎるリスクを説明できる。

### Phase 7: 成果物と完了確認

#### 7-1. データ投入手順書（45分）

- **依存:** 2-5
- **作業:** 安全ガード、profile、再投入、検証SQLをMarkdown化する。
- **完了条件:** 別セッションでも同じデータセットを再現できる。

#### 7-2. 実行計画記事（60分）

- **依存:** 4-7
- **作業:** 3クエリ以上のbefore/afterと、インデックス変更の根拠・副作用をまとめる。
- **完了条件:** Issue #62から参照できる記事原稿になっている。

#### 7-3. 設計判断の整理（45分）

- **依存:** 5-4、6-4、7-2
- **作業:** OFFSET/keyset、N+1/batch、接続プール、追加・削除しなかったインデックスの理由を記載する。
- **完了条件:** 各判断を実測値に基づいて説明できる。

#### 7-4. 最終検証（60分）

- **依存:** 7-1、7-3
- **作業:** migration down/up、small再投入、整合性検査、`go test ./... -count=1`を実行する。
- **完了条件:** 通常テストがPASSし、検証専用処理が通常DBで動かない。

#### 7-5. Definition of Done照合（30分）

- **依存:** 7-4
- **作業:** Issue #62の完了条件を一項目ずつ確認し、残課題を後続Issueへ分離する。
- **完了条件:** 全項目に成果物または実測結果へのリンクがある。

## 5. TDDとコミット方針

実装を伴うミニタスクは、次の順で進める。

1. スコープと完了条件を再確認する。
2. 整合性条件または期待動作をテストとして先に表現する。
3. Redを確認する。
4. 最小実装でGreenにする。
5. 重複や責務を整理する。
6. 関連テストを再実行する。
7. 引き継ぎジャーナルを更新してコミットする。

推奨コミット単位は次のとおり。

1. `test(perf): define seed validation queries`
2. `feat(perf): add reproducible performance dataset`
3. `docs(perf): record baseline query plans`
4. `perf(db): refine order index and remove redundant indexes`
5. `test(perf): compare n-plus-one and batch loading`
6. `feat(db): configure connection pool`
7. `docs(perf): publish issue 62 findings`

## 6. 計測ルール

- `EXPLAIN (ANALYZE, BUFFERS, FORMAT TEXT)`を使用する。
- データ件数、実行SQL、パラメータ、インデックス、PostgreSQLバージョンを併記する。
- ウォームアップ後5回実行し、中央値を代表値とする。
- OSキャッシュを強制削除したコールド計測は対象外とする。
- 実行時間だけでなくscan type、cost、actual time、rows、loops、buffersを保存する。
- 実行時間の差を自動テストのPASS条件にしない。

## 7. Definition of Done

- 3つ以上の主要クエリについてbefore/afterの実行計画がある。
- インデックス変更または変更しなかった判断を実行計画に基づいて説明できる。
- 複合インデックスのカラム順序を説明できる。
- N+1版とbatch版をクエリ回数と実行時間で比較している。
- OFFSETとkeyset paginationの差を説明できる。
- `sql.DB`接続プール設定の採用理由を説明できる。
- DB設計ガイドラインが `doc/` のMarkdownに整理されている。
- `go test ./... -count=1`がPASSする。


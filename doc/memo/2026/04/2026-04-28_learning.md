# 学習記録 2026-04-28

## セッション 1 (03:31)

### 次回課題
1. `#65` のログレベル運用定義を確定する（DEBUG/INFO/WARN/ERROR の基準、PII マスキング方針、必須ログ属性の決定）。
2. `#66` を実装タスクに落とす（ミドルウェアでの `request_id` / `trace_id` 生成・伝播、ヘッダ受信の優先ルール、context キーの設計）。
3. ロガー注入の設計を行う（選択肢: `ErrorHandler(logger, toHTTP)` として明示的注入、あるいは `pkg/logging` の singleton を用いる）。
4. `ErrorHandler` を中央ログ出力処理に拡張する（構造化 JSON、出力項目: method, route, path, status, error_type, operation, client_ip, request_id, trace_id, user_id）。
5. ログ出力のユニット/統合テストを追加する（ログが一度だけ出る、マスクが有効、レベル判定が正しいことを検証）。
6. リポジトリ内の `RespondWithError`/`RespondError` の残存参照を完全に掃除する（必要なら PR を作成）。
7. `doc/task.md` または `doc/planning` に今回の TODO を一枚化して、Issue との紐付けを明確にする（PR は doc-editor に委譲可能）。

